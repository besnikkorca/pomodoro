import { createNamespacedHelpers } from 'vuex';

const { mapGetters, mapState, mapActions, mapMutations } = createNamespacedHelpers('pomodoro');

export { mapGetters, mapState, mapActions, mapMutations };
